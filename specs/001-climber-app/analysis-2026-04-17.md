# 跨文件一致性分析報告

**日期**：2026-04-17T02:25:00+08:00
**分析範圍**：`specs/001-climber-app/` + `.kiro/specs/climber-app/` + `src/` + `package.json`
**前次分析**：`specs/001-climber-app/analysis.md`（2026-04-16）

---

## 執行摘要

自上次分析（2026-04-16）以來，**技術棧衝突（C1/I1/I3）已實質解決**：`src/` 目前為 React + Vite 實作，`package.json` 確認使用 `firebase`、`recharts`、`react-i18next`，與 `specs/001-climber-app/spec.md` 及 `plan.md` 一致。然而，`.kiro/specs/climber-app/` 文件仍描述 React Native + SQLite 架構，形成新的文件漂移問題。

---

## 發現項目

| ID | 類別 | 嚴重度 | 位置 | 摘要 | 建議 |
|----|------|--------|------|------|------|
| **C1** | 架構衝突 | CRITICAL | `.kiro/specs/climber-app/requirements.md` + `.kiro/specs/climber-app/tasks.md` | `.kiro` 文件仍描述 React Native + expo-sqlite + expo-localization 架構；實際 `src/` 為 React Web + Firebase + react-i18next。兩套文件描述不同系統。 | 更新 `.kiro/specs/` 以反映現行 Web 架構，或明確標記為已封存。 |
| **C2** | 架構衝突 | HIGH | `package.json.expo.bak` + `babel.config.js` + `app.json` | 專案根目錄仍存在 Expo 殘留配置（`app.json`、`babel.config.js`、`package.json.expo.bak`）。這些檔案可能干擾建置工具並造成混淆。 | 移除或移至 `archive/` 目錄。 |
| **I1** | 不一致 | HIGH | `specs/001-climber-app/data-model.md` vs `src/` | `data-model.md` 定義 SQLite schema（`CREATE TABLE climbs`）；實際 `src/services/` 使用 Firebase Firestore。持久化層完全不同。 | 更新 `data-model.md` 以反映 Firestore 集合結構，移除 SQLite schema。 |
| **I2** | 不一致 | HIGH | `specs/001-climber-app/research.md` vs `src/` | `research.md` 研究 `expo-sqlite` 遷移模式與 `expo-localization`；實際使用 Firestore 與 `react-i18next`。研究結論已失效。 | 補充 Firestore 資料設計與 `react-i18next` 設定研究，或標記舊研究為已棄用。 |
| **I3** | 不一致 | MEDIUM | `specs/001-climber-app/tasks.md` vs 實際進度 | `tasks.md` 所有任務（T001–T040）標記為 `[x]` 完成，但 `daily-2026-04-17.md` 指出技術棧清理與功能驗證仍為「進行中」。完成狀態不可信。 | 重新審核 tasks.md 完成狀態，或以 `.kiro/specs/climber-app/tasks.md` 為準（未完成項目更清晰）。 |
| **I4** | 不一致 | MEDIUM | `specs/001-climber-app/research.md` | `research.md` 採用 `gemini-1.5-flash`；`specs/001-climber-app/spec.md` 與 guidelines 指定 `Gemini 3.0 (Flash)`。模型版本不一致。 | 統一為 `Gemini 3.0 (Flash)`（即 `gemini-2.0-flash`），更新 `research.md` 與 `geminiClient.ts`。 |
| **G1** | 覆蓋缺口 | HIGH | `src/` | `src/` 中無 `.env` 或 `.env.local` 配置 Gemini API key。`package.json` 有 `@google/generative-ai` 依賴，但 API key 來源未定義。 | 建立 `.env.local` 並設定 `VITE_GEMINI_API_KEY`（Vite 環境變數前綴為 `VITE_`，非 `EXPO_PUBLIC_`）。 |
| **G2** | 覆蓋缺口 | MEDIUM | `specs/001-climber-app/spec.md` | spec.md 假設「v1 為單一使用者，無帳號系統（或使用 Google Auth）」，但 `package.json` 已包含 `firebase`（含 Auth 模組）。Auth 範圍未決。 | 明確決定 v1 是否啟用 Firebase Auth，更新 spec.md 假設。 |
| **G3** | 覆蓋缺口 | MEDIUM | `src/` | `src/` 目錄結構與 guidelines 定義的結構基本一致，但缺少 `src/store/`（狀態管理）與 `src/shared/utils/gradeUtils.ts`（架構規則要求）。 | 確認 `gradeUtils.ts` 是否已實作於其他路徑；補充 `store/` 或確認使用 Context API 無需獨立目錄。 |
| **A1** | 模糊性 | MEDIUM | `specs/001-climber-app/spec.md` | spec.md 假設「Gemini API key 透過環境配置提供」，但未指定環境變數名稱。Vite 專案需使用 `VITE_` 前綴，與 Expo 的 `EXPO_PUBLIC_` 不同。 | 在 spec.md 或 README 中明確記載 `VITE_GEMINI_API_KEY`。 |
| **D1** | 文件重複 | LOW | `specs/001-climber-app/analysis.md` vs 本報告 | 舊版 `analysis.md`（2026-04-16）的 CRITICAL 問題（C1 技術棧衝突）已部分解決，但文件未更新，仍顯示為未解決。 | 將舊 `analysis.md` 標記為 `[已封存]` 或刪除，以本報告為準。 |

---

## 與前次分析比較

| 前次問題 | 狀態 | 說明 |
|----------|------|------|
| C1 技術棧衝突（Web vs RN） | ✅ 實質解決 | `src/` 已為 React Web；但 `.kiro/` 文件未同步 |
| C2 `.js` 副檔名問題 | ✅ 解決 | `src/` 全為 `.ts`/`.tsx` |
| C3 DB Migrations 無任務 | ✅ 解決 | tasks.md T010 已涵蓋（雖為 SQLite，需更新為 Firestore） |
| I1 localStorage vs SQLite | ✅ 解決 | 已改為 Firestore |
| G1 zh-TW 無任務 | ✅ 解決 | `react-i18next` 已在依賴中 |
| G5 gradeUtils 無任務 | ⚠️ 待確認 | 需確認 `src/shared/` 是否已有實作 |

---

## 優先行動項目

1. **[CRITICAL]** 更新或封存 `.kiro/specs/climber-app/` — 描述的是已棄用的 React Native 架構
2. **[HIGH]** 更新 `data-model.md` — 移除 SQLite schema，補充 Firestore 集合設計
3. **[HIGH]** 設定 `VITE_GEMINI_API_KEY` 環境變數（見 G1）
4. **[HIGH]** 清理 Expo 殘留檔案（`app.json`、`babel.config.js`、`package.json.expo.bak`）
5. **[MEDIUM]** 統一 Gemini 模型版本為 `gemini-2.0-flash`（Gemini 3.0 Flash）
6. **[MEDIUM]** 決定 Firebase Auth 範圍並更新 spec.md 假設

---

## 文件健康度

| 文件 | 狀態 | 問題 |
|------|------|------|
| `specs/001-climber-app/spec.md` | ✅ 基本準確 | Auth 假設模糊；API key 名稱未定義 |
| `specs/001-climber-app/plan.md` | ✅ 基本準確 | 部分舊 RN 內容殘留（plan.md 有兩段重複架構描述） |
| `specs/001-climber-app/data-model.md` | ❌ 過時 | 描述 SQLite；實際為 Firestore |
| `specs/001-climber-app/research.md` | ❌ 部分過時 | expo-sqlite / expo-localization 研究已失效 |
| `specs/001-climber-app/tasks.md` | ⚠️ 不可信 | 全部標記完成但實際未驗證 |
| `.kiro/specs/climber-app/requirements.md` | ❌ 架構過時 | 描述 React Native；需更新或封存 |
| `.kiro/specs/climber-app/tasks.md` | ❌ 架構過時 | 描述 Expo 任務；需更新或封存 |
| `specs/001-climber-app/checklists/requirements.md` | ✅ 最新 | 昨日產出，仍有效 |
